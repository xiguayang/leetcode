import java.util.Set;

/**
 * Design a HashSet without using any built-in hash table libraries. Implement
 * MyHashSet class: void add(key) Inserts the value key into the HashSet. bool
 * contains(key) Returns whether the value key exists in the HashSet or not.
 * void remove(key) Removes the value key in the HashSet. If key does not exist
 * in the HashSet, do nothing.
 */
public class MyHashSet {
    /** Initialize data sturcture */
    public MyHashSet(){
        
    }

    public void add(int key) {
        if(!contains(key)){
            
        }
    }

    public void remove(int key) {

    }

    /** Returns true if this set contains the specified element */
    public boolean contains(int key) {

    }
}
